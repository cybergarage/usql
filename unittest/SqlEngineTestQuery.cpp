#include <boost/test/unit_test.hpp>

BOOST_AUTO_TEST_CASE(SQLQueryTest)
{
    /*
     Window window;
     
     window.setTitle(kCgGeometoryUiTestCaseWindowName);
     
     BOOST_CHECK_EQUAL(window.getWidth(), kWindowDefaultWidth);
     BOOST_CHECK_EQUAL(window.getHeight(), kWindowDefaultHeight);
     
     //window.display();
     
     //boost::this_thread::sleep(boost::posix_time::seconds(10)); 
     
     //window.close();
     */   
}